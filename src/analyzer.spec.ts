describe('getCommitSummaries', () => {
  test('is awesome', () => {});
});

describe('getUserContribution', () => {
  test('is awesome', () => {});
});

describe('analyzeTimeSpent', () => {
  test('analyzes', () => {});
});

describe('oldestLastSorter', () => {
  test('works', () => {});
});

describe('getDiffInMinutes', () => {
  test('works', () => {});
});

describe('getDateInfo', () => {
  test('works', () => {});
});

describe('asISOday', () => {
  test('works', () => {});
});
